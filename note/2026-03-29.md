## 加密解密字段介绍

消息接受代码，代码原理介绍，主要是消息字段的打印和分析：

``` python
@app.post("/wechat")
async def handle_message(request: Request, background_tasks: BackgroundTasks):
    """接收用户消息的主入口"""
    body = await request.body()
    
    # 1. 解析加密 XML，解密原理：使用内置的 Expat 解析器，将 XML 字符串一次性解析为一棵由 Element 节点组成的树，返回根节点供后续遍历、查询或修改。
    xml_tree = ET.fromstring(body)
    encrypt_content = xml_tree.find("Encrypt").text
    
    # print("encrypt_content:", encrypt_content)  # 调试输出加密内容
    # 2. 解密
    # 修改点：解密返回的是元组，我们只需要第一个值 plain_xml
    plain_xml, _ = crypt.decrypt(encrypt_content)
    
    print("plain_xml:", plain_xml)  # 调试输出解密后的明文
    if not plain_xml:
        return "decrypt error"

    msg_tree = ET.fromstring(plain_xml)
    print("msg_tree:", ET.tostring(msg_tree, encoding='utf-8').decode('utf-8'))  # 调试输出消息树内容
    user_id = msg_tree.find("FromUserName").text
    content = msg_tree.find("Content").text

    print("user_id:", user_id)  # 调试输出用户 ID
    print("content:", content)  # 调试输出消息内容

    # 3. 核心：异步处理
    background_tasks.add_task(process_ai_task, user_id, content)
    
    return "success" 

```


plain_xml: 
``` xml
<xml><ToUserName><![CDATA[wwda5759879600d079]]></ToUserName><FromUserName><![CDATA[LuDunHang]]></FromUserName><CreateTime>1774774772</CreateTime><MsgType><![CDATA[text]]></MsgType><Content><![CDATA[你好呀]]></Content><MsgId>7622599607031511130</MsgId><AgentID>1000002</AgentID></xml>

```

msg_tree: 
``` xml
<xml><ToUserName>wwda5759879600d079</ToUserName><FromUserName>LuDunHang</FromUserName><CreateTime>1774774772</CreateTime><MsgType>text</MsgType><Content>你好呀</Content><MsgId>7622599607031511130</MsgId><AgentID>1000002</AgentID></xml>
```

user_id: LuDunHang
content: 你好呀